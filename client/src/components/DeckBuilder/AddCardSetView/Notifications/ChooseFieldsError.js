const ChooseFieldsError = () => {
  return(
    <div>
      <strong>Invalid fields</strong>
      <p>All required fields are not defined or the data in some field is not the expected type</p>
    </div>
  )
}

export default ChooseFieldsError